# Godfather Session — 2026-04-01 (2:30 AM – 5:00 AM)

## What We Did

### 1. Data Architecture Rebuild
- Built `getAdPerformanceDaily()` — daily ad-level join of Meta API spend + CRM leads
- Rebuilt `getAdPerformance()` to aggregate from daily layer (single source of truth)
- Fixed CRM↔Meta join using `mx_utm_term` as alias key (88% match rate at aggregate level)
- Added `actions`/`cost_per_action_type` to Meta API backfill fields
- Fixed 4 critical bugs: double-counting, object mutation, Meta≠CRM QL mixing, date filtering

### 2. Dashboard Fixes
- Removed Market Health + Market Intelligence sections (empty, numbers didn't match)
- Added notes/feedback input to every Pause Now and Make More card
- Fixed Make More to exclude Pause Now ads (Madhavi contradiction)
- Date filter now respected by Pause Now (removed forced 90-day lookback)
- Bumped font sizes on oracle cards

### 3. Audit
- Full visual audit across all 8 views → AUDIT-2026-04-01.md
- Cross-checked numbers against Excel (US LP campaigns)
- Identified root cause of spend inflation: campaign_prorated fallback (40x inflation for some ads)

## Current State

### Data Flow
```
Meta API (daily) → metaAdData (32K rows)
CRM (Google Sheet) → leadsData (9K rows)
                  ↓
    getAdPerformanceDaily() — 31K daily rows
    (join on normalizedAdName + date, utm_term alias)
                  ↓
    getAdPerformance() — 2,322 ads aggregated
    (2,108 meta_insights, 214 no_spend, 0 campaign_prorated)
                  ↓
    All ad-level views: Pause Now, Make More, Performance, Tagger
```

### Numbers Consistency (Nov 2025 – Mar 2026)
| Source | Spend | QL | TD |
|--------|-------|-----|-----|
| Dashboard (raw CRM) | ₹9.2Cr | 8,710 | 2,217 |
| getAdPerformance | ₹9.0Cr | 7,857 | 1,796 |
| Daily layer | ₹9.2Cr | 7,857 | 1,796 |
| Gap reason | Meta API vs costData | 853 leads no ad name | 421 TDs no ad name |

QL/TD match perfectly between daily and aggregated ✅
Dashboard shows full CRM counts (includes leads without ad attribution) ✅
No more campaign_prorated inflation ✅

## Still To Do

### P0 — Data
- [ ] Re-run backfill to populate `leads` field in metaAdData (actions from Meta API)
- [ ] Wire Tagger to use getAdPerformance instead of state.taggerData (currently shows stale data: 13,287 QL vs 7,857)
- [ ] Wire Performance view KPIs to use getAdPerformance spend (currently reads costData)

### P1 — Weekly Trend Signals
- [ ] Pause Now: "This ad spent ₹X this week with 0 leads. Last week: Y leads. Trend: declining."
- [ ] Make More: "This ad improved from ₹14K CPTQL last week to ₹8K this week."
- [ ] Insights: Weekly cohort view — which hooks/formats are trending up/down

### P2 — UI/UX
- [ ] Tag migration for intermediate taxonomy values
- [ ] Thumbnail coverage improvement
- [ ] Create tab ICP types → new taxonomy
- [ ] Remove CMO Decisions section (overlaps Insights)

### P3 — New Views
- [ ] Data Explorer: daily ad-level table (like Ads Manager) with date/spend/QL/CPTQL per row
- [ ] Weekly Performance Report: auto-generated summary of what changed week-over-week

## Commits
1. `92199c7` — Fix data architecture: Meta API primary, cross-exclusion, notes, fonts
2. `896c593` — Fix CRM↔Meta join via utm_term alias (88% match)
3. `112f907` — Fix 4 critical bugs in join logic
4. `161e923` — Add getAdPerformanceDaily() foundation
5. `c31a49a` — Rebuild getAdPerformance() to aggregate from daily
